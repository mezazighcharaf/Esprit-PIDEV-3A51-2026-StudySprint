{% extends 'layouts/bo.html.twig' %}

{% block title %}Modifier Quiz - Administration{% endblock %}
{% block breadcrumb %}Training > Quiz > Modifier{% endblock %}

{% block content %}
<div class="page-header">
    <h1>Modifier le Quiz</h1>
    <p class="text-muted">{{ quiz.title }}</p>
</div>

{% for message in app.flashes('success') %}
    <div class="alert alert-success">{{ message }}</div>
{% endfor %}

<div class="card">
    <div class="card-body">
        {{ form_start(form, {'attr': {'class': 'form'}}) }}
            <div class="form-group">
                {{ form_label(form.title) }}
                {{ form_widget(form.title, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.title) }}
            </div>

            <div class="form-group">
                {{ form_label(form.difficulty) }}
                {{ form_widget(form.difficulty, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.difficulty) }}
            </div>

            <div class="form-actions">
                <a href="{{ path('admin_training_quiz_show', {id: quiz.id}) }}" class="btn btn-secondary">Annuler</a>
                <button type="submit" class="btn btn-primary">Enregistrer</button>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
